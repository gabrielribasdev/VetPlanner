import React from "react";
import Layout from "../components/layout/layout";

const Home: React.FC = () => {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
};

export default Home;
